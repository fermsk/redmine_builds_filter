<h2><%= l(:label_build_new) %></h2>

<%= form_for [@project, @build] do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= hidden_field_tag 'project_id', @project.id if @project %>
  <%= submit_tag l(:button_create) %>
<% end %>